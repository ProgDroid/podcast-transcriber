# Transcript data — regenerated by the pipeline; also lives in the Modal
# volume `podcast-transcripts` and in Chroma. Remove this line to version them.
downloaded/

# Python
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Virtual envs
.venv/
venv/
env/
ENV/

# Secrets / credentials — never commit
.env
*.env
creds.ps1
*.key
*.pem

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Agent skill caches (vendored upstream docs, not ours to ship)
.agents/
.claude/skills/

# Local tool state
.serena/

# SDD scratch workspace (ledger, briefs, review packages) -- regenerable
.superpowers/
